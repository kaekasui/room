= form_for @ticket_category do |f|
  - if @ticket_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@ticket_category.errors.count, "error")} prohibited this ticket_category from being saved:"
      %ul
        - @ticket_category.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
