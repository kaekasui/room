%h1 Listing priorities

%table
  %tr
    %th Name
    %th Position
    %th Deleted at
    %th
    %th
    %th

  - @priorities.each do |priority|
    %tr
      %td= priority.name
      %td= priority.position
      %td= priority.deleted_at
      %td= link_to 'Show', priority
      %td= link_to 'Edit', edit_priority_path(priority)
      %td= link_to 'Destroy', priority, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Priority', new_priority_path
