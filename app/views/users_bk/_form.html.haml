= form_for @user, html: {class: "form-horizontal"} do |f|
  .control-group
    = f.label :email, class: "control-label"
    = asterisk(@user, :email)
    .controls
      = f.text_field :email
  .control-group
    .controls
      = f.submit class: "btn"
