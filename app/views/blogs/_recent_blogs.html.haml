.well
  %h4
    = image_tag icon, class: "icon"
    = t("side_menu.recent_blogs")
  %ul.list-unstyled
    - @recent_blogs.each do |blog|
      %li= link_to blog.title + "(" + l(blog.posted_at, format: :very_short) + ")", blog
