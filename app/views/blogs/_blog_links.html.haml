.well.sidebar-nav
  %h4.sidebar_title
    = image_tag icon, class: "icon"
    = t("side_menu.blog_links")
  %ul.nav.nav-list.sidebar_blog_links
    - @blog_links.each do |link|
      %li
        %a{href: "#{link.url}", target: "_blank", rel: "tooltip", "data-original-title" => "#{link.author}#{I18n.t("blog_link.prefix")}：#{link.introduction}", "data-placement" => "left"}= link.title
