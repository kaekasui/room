require "spec_helper"

describe Admin::TrackersController do
  describe "routing" do
=begin
    it "routes to #index" do
      get("/trackers").should route_to("trackers#index")
    end

    it "routes to #new" do
      get("/trackers/new").should route_to("trackers#new")
    end

    it "routes to #show" do
      get("/trackers/1").should route_to("trackers#show", :id => "1")
    end

    it "routes to #edit" do
      get("/trackers/1/edit").should route_to("trackers#edit", :id => "1")
    end

    it "routes to #create" do
      post("/trackers").should route_to("trackers#create")
    end

    it "routes to #update" do
      put("/trackers/1").should route_to("trackers#update", :id => "1")
    end

    it "routes to #destroy" do
      delete("/trackers/1").should route_to("trackers#destroy", :id => "1")
    end
=end
  end
end
